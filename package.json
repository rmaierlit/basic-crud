{
  "name": "crud",
  "version": "1.0.0",
  "description": "project with api performing basic CRUD actions in mysql database",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rmaierlit",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mysql": "^2.18.1"
  }
}
